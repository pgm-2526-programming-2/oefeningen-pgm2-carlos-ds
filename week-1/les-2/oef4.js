/* Write a function named minutesToHours that receives a number of minutes 
(larger than 0) as parameter and returns a string representing the amount of hours 
and minutes. 
e.g. minutesToHours(150) -> 2 hours and 30 minutes
minutesToHours(120) → 2 hours
minutesToHours(40) → 40 minutes
*/
